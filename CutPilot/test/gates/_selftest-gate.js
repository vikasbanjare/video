/* Proves gate discovery itself works: this gate always passes. Break it (exit 1)
   to confirm the runner reports a FAIL — see README.md in this folder. */
console.log('gate discovery: the runner found and ran this file ✓');
process.exit(0);
